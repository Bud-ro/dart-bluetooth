<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Legacy Bluetooth permissions (API <= 30). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+ (API 31+) runtime Bluetooth permissions. BLE scanning does
         not derive location, so declare neverForLocation. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- BLE scan on API <= 30 required location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Filter installs to devices with BLE hardware. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="true" />
</manifest>
