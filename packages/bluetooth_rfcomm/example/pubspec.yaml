name: bluetooth_rfcomm_cli_example
description: Pure-Dart CLI demo for bluetooth_rfcomm (no Flutter).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  bluetooth_rfcomm:
    path: ../
  args: ^2.5.0

dev_dependencies:
  lints: ^5.0.0
